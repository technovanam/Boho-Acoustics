import * as Icons from 'lucide-react';
console.log(Object.keys(Icons).filter(icon => icon.toLowerCase().includes('insta')));
console.log(Object.keys(Icons).filter(icon => icon.toLowerCase().includes('linke')));
