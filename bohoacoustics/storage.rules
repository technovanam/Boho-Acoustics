rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /consultation_files/{consultationId}/{allPaths=**} {
      allow write: if false;
      allow read: if request.auth != null;
    }
  }
}
