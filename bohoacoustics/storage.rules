rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /consultation_files/{allPaths=**} {
      allow write: if true;
      allow read: if request.auth != null;
    }
  }
}
